// Temporary stub - will be replaced in Task 10
interface Props {
  onClose: () => void
  onManual: () => void
  onScanDone: (barcode: string, name: string | null, category: string | null) => void
}
export default function AddItemModal(_props: Props) {
  return null
}
